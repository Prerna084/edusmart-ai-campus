import 'package:flutter/material.dart';

class AppColors {
  // Primary Gradient (Now Solid Warm)
  static const Color primaryStart = Color(0xFFCC8966); // Caramel
  static const Color primaryEnd = Color(0xFFE2A687); // Lighter caramel

  // Background
  static const Color background = Color(0xFFF7EFE8); // Creamy Beige
  static const Color surface = Color(0xFFFAF6F2); // Lighter Surface
  static const Color glassBorder = Color(0x333E2A1E); // Soft dark shadow instead of white

  // Accent Colors
  static const Color accent = Color(0xFFD9B9A6); // Warm Accent
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Text
  static const Color textPrimary = Color(0xFF3E2A1E); // Espresso Dark
  static const Color textSecondary = Color(0xFF8D7668); // Muted brown
  static const Color textMuted = Color(0xFFB5A196); // Lighter muted brown
}
